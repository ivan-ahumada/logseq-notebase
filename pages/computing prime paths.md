---
title: Computing prime paths
---

## Find all [[Simple path]]s:
### Meaning find all [[Simple path]]s of length $0$ and extend them to length $1$, then length $2$, and so on.
### Select those that are maximal
## **CAREFUL**: `!` in a path table means to not consider that node because it doesn't have outgoing edges. Also used if a path would repeat nodes on a further extend. `*` indicates a cycle and it cannot be extended for the next step.
## Any
